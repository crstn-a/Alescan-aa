path: ./data            # root of the exported Roboflow dataset
train: train/images     # relative to path above
val: valid/images
test: test/images       # optional — used only for final eval

nc: 3                   # number of classes — do not change
names:
  0: whole_chicken      # must match LABELS dict in vision.py
  1: tilapia_local
  2: pork_liempo